# 放入 /etc/nginx/conf.d/websocket-map.conf（文件名自定，扩展名 .conf）
# 与 host-reverse-proxy.conf 中 $connection_upgrade 配合使用。
# 仅需一份，在 http {} 内加载（conf.d 默认即在此上下文中）。

map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
